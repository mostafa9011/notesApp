class NoteItemModel {
  final String title, subtitle, date;
  final int color;
  const NoteItemModel({
    required this.title,
    required this.subtitle,
    required this.date,
    required this.color,
  });
}
